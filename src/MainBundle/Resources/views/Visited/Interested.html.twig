{% extends 'Base.html.twig' %}
{% block ShowVisited %}
    <section id="reservation" class="reservation_section bg-grey">
        <div class="container padding">
            <div class="col-sm-10 col-sm-offset-1 align-left">
                <div class="reserv_form">
                    <div class="section_heading mb-40">


                        <h2>Les évenements qui m'intéressent</h2>
                        <table >
                            <tr>
                                <th width="30%" >Evenement</th><th width="30%">Marquer going</th><th width="30%">Marquer non intéressé</th>
                            </tr>
                            {% for vis in interest %}
                                <tr>
                                    <td>{{vis.event.nom  }}</td>
                                    <td><a href="{{ path('ajouter_going_event_profile',{'id_event':vis.id}) }}">   X</a></td>
                                    <td><a href="{{ path('delete_interest_event_profile',{'id_event':vis.id}) }}">   X</a></td>
                                </tr>
                            {% endfor %}
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}
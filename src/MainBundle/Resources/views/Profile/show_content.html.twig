{% trans_default_domain 'FOSUserBundle' %}


<div class="fos_user_user_show">

    <p>{{ 'profile.show.username'|trans }}: {{ user.username }}</p>
    <p>{{ 'profile.show.email'|trans }}: {{ user.email }}</p>

    <div>
        <p><strong>Pseudonyme:</strong> {{ (app.user.username) }}</p>

<head>
<link href="{{ asset('bundles/main/BackEnd/css/style.css')}}" rel="stylesheet">
<link href="{{ asset('bundles/main/BackEnd/css/bootstrap.min.css')}}" rel="stylesheet">
<link href="{{ asset('bundles/main/BackEnd/assets/font-awesome/css/font-awesome.css')}}" rel="stylesheet" />


<style>
    .BAC
    {
        width: 100%;
    }
</style>
<style>
    #ABCD
    {
        text-align: left;
        font-style: normal;
        padding: 10px;
        background: rgba(80,86,82,0.65);
        font-size: 16px;
        font-weight: 500;
    }
</style>
</head>

<section id="reservation" class="reservation_section bg-grey">
    <div class="container col-sm-offset-0 col-lg-6">
<section class="wrapper">
        <!-- page start-->
        <div class="row">
            <aside class="profile-info col-lg-12">
                    <button type="button" class="btn btn-default"><a href="javascript:history.go(0)"><i class="fa fa-user"></i> Page </a></button>
                    <button class="btn btn-default"><a href="#"><i class="fa fa-thumb-tack"></i> Visités </a></button>
                    <button class="btn btn-default"><a href="#"><i class="fa fa-thumb-tack"></i> Wishlist </a></button>
                    <button class="btn btn-default"><a href="#"><i class="fa fa-thumb-tack"></i> Going Events </a></button>
                    <button class="btn btn-default"><a href="#"><i class="fa fa-thumb-tack"></i> Interested Events </a></button>
                <section class="panel">
                    <div class="panel-body bio-graph-info">
                        <div class="row">
                            <div class="bio-row BAC">
                                <p><span><strong>Pseudo</strong></span>: {{ user.username }}</p>
                            </div>
                            <div class="bio-row BAC">
                                <p><span><strong>E-Mail</strong></span>: {{ user.email }}</p>
                            </div>
                            <div class="bio-row BAC">
                                <p><span><strong>Nom & Prénom</strong></span>: {{ user.nom }}</p>
                            </div>
                            <div class="bio-row BAC">
                                <p><span><strong>Sexe</strong></span>: {{ user.sexe }}</p>
                            </div>
                            <div class="bio-row BAC">
                                <p><span><strong>Date De Naissance</strong></span>: {{ user.datedenaissance | date('y-m-d') }}</p>
                            </div>
                            <div class="bio-row BAC">
                                <p><span><strong>Ville</strong></span>: {{ user.ville }}</p>
                            </div>
                            <div class="bio-row BAC">
                                <p><span><strong>Numéro</strong></span>: {{ user.phone }}</p>
                            </div>
                            <div class="bio-row BAC">
                                <p><span><strong>Site Web/Blog</strong></span>:<a href="{{ user.url }}">{{ user.url }}</a></p>
                            </div>
                        </div>
                    </div>
                </section>
                <div class="bio-graph-heading" id="ABCD">
                    <p><span><strong>Intro : </strong></span>{{ user.intro }}</p>
                </div>
            </aside>

            </div>

        <!-- page end-->
    </section>
    </div>
</section>
    </div>



</div>

</section>



{% extends 'baseAdmin.html.twig' %}

{% block Photo %}
    <section id="main-content">
        <section class="wrapper">
            <div class="row">
                <div class="col-lg-12 col-sm-offset-0">
                    <section class="panel" id="ABC">
                        <header class="panel-heading col-sm-offset-0" id="ABCDE">
                            <strong>{{nom}}</strong>
                        </header>
                        <header class="panel-heading">
                            {{ form_start(etabl) }}
                            <table>
                                <tr>
                                    <td>
                                        {{ form_label(etabl.image_principale) }}
                                    </td>
                                    <td>
                                        {{ form_widget(etabl.image_principale) }}
                                    </td>
                                </tr>
                                <tr>
                                    <td>

                                    </td>
                                    <td>
                                        {{ form_end(etabl) }}
                                    </td>
                                </tr>
                                <tr>
                                    <td>

                                    </td>
                                    <td>
                                        <strong><input value="Confirmer Les Changements" class="btn btn-shadow btn-success" type="submit"></strong>
                                    </td>
                                </tr>
                            </table>
                        </header>
                    </section>
                </div>
            </div>
        </section>
    </section>
{% endblock %}